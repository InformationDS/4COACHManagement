<view class="tabbar">
  <block wx:for="{{tabs}}" wx:key="pagePath">
    <view class="tabbar-item {{selected === index ? 'active' : ''}}" data-index="{{index}}" bindtap="switchTab">
      <text>{{item.text}}</text>
    </view>
  </block>
</view>
