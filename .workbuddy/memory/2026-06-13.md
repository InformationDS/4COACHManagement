# 2026-06-13

## 教练单人模式优化计划
- 用户确认下一版定位为“教练单人模式”，暂不开放学员端。
- 已将优化计划写入 `OPTIMIZATION_PLAN.md`。
- 优先级：
  - P0：修复排课状态、完成课程扣课时事务、训练记录 upsert、教练工作时间设置生效、学员课程历史查询。
  - P1：收敛学员端残留入口、清理调试代码和订阅消息主流程残留。
  - P2：强化课程页今日操作台、学员详情页、训练记录复用体验。
  - P3：拆分 API、统一状态配置、补索引和检查脚本。

## 本批执行目标
- 修复 `createLesson` 覆盖传入状态的问题。
- 新增 `completeLesson` 云函数，用事务处理课程完成、扣课时、写课时日志。
- 教练端完成课程改为调用云函数。
- 训练记录保存改为按 `lesson_id` upsert。
- 教练端排课弹窗读取每日工作时间设置。
- 学员详情页课程历史改为直接按学员查询并移除 DEBUG 日志。
- 清理单人模式下不可达的学员分享邀请逻辑。
- 新增 `PRD_COACH_SOLO.md`，明确当前版本不开放学员端。
- 删除废弃 `components/student-tabbar` 组件。
