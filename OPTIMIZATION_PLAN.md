# 私教小程序教练单人模式优化计划

> 日期：2026-06-13  
> 目标：将当前双端 MVP 收敛为教练单人可用版本，优先保证核心闭环稳定、交互直观、后续可扩展。

## 1. 优化目标

教练单人模式的核心闭环是：

1. 添加学员
2. 给学员排课
3. 标记课程完成并扣课时
4. 记录训练内容
5. 在学员档案中回看课程、课时和训练记录

本阶段不开放学员端自助约课、学员绑定、双向确认和订阅消息。

## 2. P0：核心正确性

- 修复排课状态覆盖问题：教练新建课程应直接进入 `confirmed`，不能被底层 `createLesson` 覆盖为 `pending`。
- 完成课程改为云函数事务：课程状态更新、课时扣减、课时日志写入必须保持一致，并防止重复扣课。
- 训练记录保存改为 upsert：已有记录时更新，避免一节课产生多条训练记录。
- 教练排课读取工作时间设置：`daily_start_time` / `daily_end_time` 应传入 `time-grid`。
- 学员近期课程直接按 `student_id` 查询，避免先拉 100 条课程再前端过滤导致漏数据。

## 3. P1：单人模式边界收敛

- 从主配置中移除或隔离学员端页面注册，避免隐藏页面被误进入。
- 禁用学员绑定分支和分享邀请逻辑。
- 清理 `student-tabbar` 等不再使用的组件。
- 清理订阅消息调用入口，保留云函数但不接入主流程。
- 移除调试日志、未使用 import 和死代码。

## 4. P2：教练体验增强

- 课程页继续强化为“今日操作台”：
  - 待完成课程置顶
  - 已完成但未写训练记录的课程提示
  - 学员课时不足提醒
  - 从课程卡快速进入训练记录
- 排课流程提速：
  - 从学员详情页发起排课并自动带入学员
  - 默认使用学员偏好地点或最近上课地点
  - 支持复制上一节课安排
- 学员详情页增强：
  - 顶部突出剩余课时、最近上课、下次课程
  - 课程历史支持直接进入课程详情/训练记录
  - 课时不足时明显提示
- 训练记录增强：
  - 常用动作快捷选择
  - 最近训练动作复用
  - 按部位聚合训练历史

## 5. P3：结构和发布准备

- 拆分 `utils/api.js`：
  - `studentApi`
  - `lessonApi`
  - `lessonCardApi`
  - `trainingRecordApi`
  - `settingsApi`
- 建立统一 `lessonStatus` 配置，集中管理状态文案、颜色、动作和占用规则。
- 补充数据库索引：
  - `lessons(coach_openid, date, start_time)`
  - `lessons(coach_openid, student_id, date)`
  - `training_records(lesson_id)`
  - `lesson_card_logs(student_id, created_at)`
- 增加轻量检查脚本：
  - JSON 校验
  - JS 语法检查
  - 搜索残留学员端入口
  - 搜索 DEBUG 日志
  - 校验 `app.json` 页面文件完整性
- 新增 `PRD_COACH_SOLO.md`，将单人模式产品边界写清楚。

## 6. 当前执行批次

本批先处理 P0 + 部分 P1：

- 修复 `createLesson` 状态覆盖
- 新增 `completeLesson` 云函数事务
- 改造教练端完成课程逻辑
- 改造训练记录 upsert
- 教练端排课读取工作时间
- 学员近期课程改为按 `student_id` 查询
- 清理学员分享邀请死代码和调试日志

